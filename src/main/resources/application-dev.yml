server:
  port: 8080


spring:
  application:
    name: cooking-app
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      # 设置临时文件目录
      location: ${java.io.tmpdir}
      # 文件上传后立即清理临时文件
      resolve-lazily: false

  # 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cooking_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root

  # Redis配置
  redis:
    host: localhost
    port: 6379
    password:
    database: 0
    timeout: 3000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms

# MyBatis Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.cooking.entity

# 微信小程序配置
wechat:
  appid: wx4bb4e5cf5c71bdb3  # 替换为你的小程序AppID
  secret: 09afea87488b51e1c58ed118328f4594  # 替换为你的小程序AppSecret

# JWT配置
jwt:
  secret: cooking-app-secret-key-2024  # JWT密钥
  expire-time: 604800000  # 过期时间（毫秒），默认7天

# MinIO配置
minio:
  endpoint: http://file.xuaq.top:19000 # 注意是9000端口（数据端口），不是控制台的9001
  accessKey: IDjlfoXFOCpf5tbTIjBm  # 你部署时设置的MINIO_ROOT_USER
  secretKey: cWDIEDnyOShCYzU8PgNT4UsmZB2BYg98wgTVe688   # 你部署时设置的MINIO_ROOT_PASSWORD
  bucketName: test  # 自定义桶名（会自动创建，无需手动在控制台创建）


# LLM大模型配置
llm:
  base-url: http://106.55.137.118/api/completion  # Nginx转发地址
  api-key: key-1d91f1cb4de3e1797907393c45c19278ac7af559  # 你的api-key
  timeout:
    connect: 10000  # 连接超时10秒
    read: 60000     # 流式读取超时60秒
  # 生成参数
  stream: true      # 默认流式响应
  max-tokens: 512
  temperature: 0.6
  top-p: 0.85
  repeat-penalty: 1.1
  stop: ["### User: ", "### Conversation End ###"]  # 停止词



# 熔断/重试配置
resilience4j:
  circuitbreaker:
    instances:
      llmService:
        failureRateThreshold: 50        # 失败率50%触发熔断
        slidingWindowSize: 10           # 统计窗口10次请求
        minimumNumberOfCalls: 5         # 最少5次请求才触发
        waitDurationInOpenState: 60s    # 熔断后60秒尝试恢复
        permittedNumberOfCallsInHalfOpenState: 3  # 半开状态允许3次请求
        recordExceptions:
          - java.io.IOException
          - org.springframework.web.client.HttpServerErrorException
          - java.lang.RuntimeException
  retry:
    instances:
      llmService:
        maxAttempts: 2                  # 最多重试1次（总计2次）
        waitDuration: 1000ms           # 重试间隔1秒
        retryExceptions:
          - java.io.IOException
          - org.springframework.web.client.HttpServerErrorException

# 日志配置
logging:
  level:
    com.ai.chat: INFO
    org.springframework.web: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"