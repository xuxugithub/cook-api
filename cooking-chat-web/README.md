# AI对话系统 - 前端项目

这是一个基于React的AI对话系统前端应用，提供用户认证、会话管理和实时对话功能。

## 功能特性

### 🔐 用户认证
- 用户注册（姓名、手机号、邮箱、密码）
- 用户登录（手机号+密码）
- 完整的表单验证
- JWT Token认证

### 💬 对话管理
- 创建新对话（最多10个）
- 查看对话历史
- 删除对话
- 切换对话

### 🤖 AI对话
- 流式响应（实时显示AI回复）
- 消息历史保存
- 支持多轮对话
- Enter发送，Shift+Enter换行

## 技术栈

- **React 18** - UI框架
- **Vite** - 构建工具
- **CSS3** - 样式
- **Fetch API** - HTTP请求
- **SSE (Server-Sent Events)** - 流式响应

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置后端地址

编辑 `vite.config.js`，确保代理配置正确：

```javascript
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',  // 后端地址
        changeOrigin: true
      }
    }
  }
})
```

### 3. 启动开发服务器

```bash
npm run dev
```

应用将在 http://localhost:3000 启动

### 4. 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist` 目录

### 5. 预览生产版本

```bash
npm run preview
```

## 项目结构

```
cooking-chat-web/
├── src/
│   ├── pages/
│   │   ├── Login.jsx          # 登录页面
│   │   ├── Register.jsx       # 注册页面
│   │   ├── ChatPage.jsx       # 对话页面
│   │   ├── Auth.css           # 认证页面样式
│   │   └── ChatPage.css       # 对话页面样式
│   ├── App.jsx                # 应用主组件
│   ├── main.jsx               # 应用入口
│   └── index.css              # 全局样式
├── index.html                 # HTML模板
├── vite.config.js             # Vite配置
├── package.json               # 项目配置
└── README.md                  # 项目文档
```

## API接口

### 认证接口

#### 注册
```
POST /api/auth/register
{
  "name": "张三",
  "phone": "13800138000",
  "email": "zhangsan@example.com",
  "password": "password123"
}
```

#### 登录
```
POST /api/auth/login
{
  "phone": "13800138000",
  "password": "password123"
}
```

### 会话接口

#### 创建会话
```
POST /api/conversation/create
Headers: token: <JWT_TOKEN>
{
  "title": "新对话"
}
```

#### 获取会话列表
```
GET /api/conversation/list
Headers: token: <JWT_TOKEN>
```

#### 获取会话消息
```
GET /api/conversation/{conversationId}/messages
Headers: token: <JWT_TOKEN>
```

#### 删除会话
```
DELETE /api/conversation/{conversationId}
Headers: token: <JWT_TOKEN>
```

### AI对话接口

#### 流式对话
```
POST /api/chat/stream
Headers: token: <JWT_TOKEN>
{
  "userId": "1",
  "question": "你好",
  "stream": true
}
```

## 表单验证规则

### 注册表单
- **姓名**：不能为空，长度不超过10位
- **手机号**：11位数字，1开头
- **邮箱**：符合标准邮箱格式
- **密码**：至少8位，不能为纯数字
- **确认密码**：必须与密码一致

### 登录表单
- **手机号**：11位数字，1开头
- **密码**：不能为空

## 本地存储

应用使用 localStorage 存储以下信息：
- `token` - JWT认证令牌
- `userInfo` - 用户基本信息

## 开发说明

### 添加新页面
1. 在 `src/pages/` 目录下创建新组件
2. 在 `App.jsx` 中添加路由逻辑
3. 创建对应的CSS文件

### 修改样式
- 全局样式：编辑 `src/index.css`
- 认证页面：编辑 `src/pages/Auth.css`
- 对话页面：编辑 `src/pages/ChatPage.css`

### 调试技巧
1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签页的日志
3. 查看 Network 标签页的网络请求
4. 查看 Application 标签页的 localStorage

## 常见问题

### 1. 无法连接后端
- 检查后端服务是否启动（默认8080端口）
- 检查 `vite.config.js` 中的代理配置
- 查看浏览器控制台的错误信息

### 2. Token验证失败
- 检查 localStorage 中是否有 token
- 尝试重新登录
- 检查 token 是否过期

### 3. 消息发送失败
- 检查是否选择了会话
- 检查网络连接
- 查看浏览器控制台错误

### 4. 样式显示异常
- 清除浏览器缓存
- 检查CSS文件是否正确导入
- 使用浏览器开发者工具检查元素样式

## 浏览器支持

- Chrome (推荐)
- Firefox
- Safari
- Edge

建议使用最新版本的现代浏览器以获得最佳体验。

## 部署

### 使用Nginx部署

1. 构建项目：
```bash
npm run build
```

2. 配置Nginx：
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    root /path/to/cooking-chat-web/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

3. 重启Nginx：
```bash
sudo nginx -s reload
```

## 许可证

MIT

## 联系方式

如有问题或建议，请联系开发团队。
